#! /bin/sh

idents="$1"
user="$2"

verb="identity_changed"
data="{ \"identities\": \"${idents}\", \"user\":\"${user}\" }"

tmp=$(mktemp /run/exec/doms/manual_XXXXXX)
echo "{\"verb\":\"${verb}\",\"data\":${data}}" > ${tmp}
chmod 444 ${tmp}
